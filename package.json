{
  "name": "logsluice",
  "version": "1.0.0",
  "description": "Log normalizer & summary CLI: mixed-format logs to canonical JSONL with quarantine and deterministic summaries",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "logsluice": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
