# PLAN — {PROJECT_ID}

**Agent/Model**: {AGENT_NAME}
**Started**: {DATE}
**Spec**: {SPEC_PATH}
**Mode**: attended / unattended (delete one)

## Understanding of the task

(3–6 sentences: what the project does, what the hard parts are.)

## Task breakdown

Format: `- [ ]` while pending, `- [x]` when done. One acceptance criterion per task. Update **as you go**, not at the end.

- [ ] T1 — Scaffold project layout per spec §Architecture; build passes
      Accept: BUILD_CHECK green with empty skeleton.
- [ ] T2 — …
      Accept: …

## Decisions & spec deviations

| # | Decision / deviation | Justification |
|---|---------------------|---------------|

## Final report (fill at the end)

- Wall-clock time:
- Total tokens consumed (in + out) + avg output t/s (if the harness exposes them; state source):
- Errors/retries (build/test/lint):
- Final coverage (number + measurement command):
- Line counts per directory:
- Deviations from spec:
