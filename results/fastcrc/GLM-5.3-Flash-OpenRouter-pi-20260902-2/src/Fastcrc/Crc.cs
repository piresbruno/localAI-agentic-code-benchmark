namespace Fastcrc;

/// <summary>Pure CRC-32 (IEEE 802.3) computation — the only algorithm module.</summary>
public static class Crc
{
    /// <summary>Computes the CRC-32 checksum of <paramref name="data"/>.</summary>
    public static uint Crc32(byte[] data) => 0;
}
