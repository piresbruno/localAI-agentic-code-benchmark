namespace Fastcrc;

/// <summary>CLI boundary: argv parsing, error envelope, exit codes, help.</summary>
public static class Cli
{
    /// <summary>Runs the CLI and returns the process exit code.</summary>
    public static int RunCli(string[] args) => 0;
}
