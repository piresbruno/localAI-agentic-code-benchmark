# Copy to .env (or export) to override local defaults. All optional for local dev.
PORT=3000
# JWT signing secret. Local default is a documented dev-only value; ALWAYS set a
# strong random secret outside local development.
JWT_SECRET=dev-only-secret-change-me
NODE_ENV=development
