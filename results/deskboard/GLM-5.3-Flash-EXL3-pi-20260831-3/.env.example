# DeskBoard server configuration (safe local defaults — see README)
# Port the API + UI are served on
PORT=3000
# Secret used to sign JWTs. Change this for anything beyond local development.
JWT_SECRET=dev-only-secret-change-me
