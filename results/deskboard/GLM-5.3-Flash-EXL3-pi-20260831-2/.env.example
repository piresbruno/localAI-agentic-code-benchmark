# Port the API + UI are served on
PORT=3000
# Secret used to sign JWTs (12h expiry). REQUIRED in production; dev default below.
JWT_SECRET=dev-only-secret-change-me
# production | development (development enables verbose error logging)
NODE_ENV=development
